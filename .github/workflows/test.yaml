---
name: Build Docker Image

on:
 pull_request: 
  branches: master

jobs:
  deploy:
  runs-on: ubuntu-latest
    name: Build Application
    steps:
      - name: Checkout code
        uses: actions/checkout@v2

      - name: Setup Py
        run: 
          cd py
   
          py test


